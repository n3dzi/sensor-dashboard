services:
  sensor-dashboard:
    build: .
    container_name: sensor-dashboard
    restart: unless-stopped
    ports:
      - "8501:8501"
    volumes:
      - sensor_db:/app/data
    environment:
      - TZ=Europe/Berlin

volumes:
  sensor_db:
